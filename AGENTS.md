# 게임 변경 시 필수 검증

사용자 요청: 게임을 수정할 때마다 자체 테스트하여 버그를 확인한다.

- 단위 테스트와 시작 화면 확인만으로 완료를 선언하지 않는다.
- 수정한 게임은 실제 브라우저 입력으로 첫 정답 이후 여러 문제/웨이브까지 연속 진행한다. 업데이트뿐 아니라 매 프레임 렌더도 검사한다.
- 실제 requestAnimationFrame 루프가 살아 있는지 확인한다. 가상 시간 또는 직접 정답 함수 호출 검사만으로 대체하지 않는다.
- 적용 가능한 정답·오답·놓침·회복·압축·웨이브 완료·피버 진입/종료·일시정지/재개·결과/재시작 경계를 검사한다. 소리 ON/OFF와 휴대폰 화면도 확인한다.
- 브라우저 예외를 수집하고, 재현한 버그에는 실패를 잡는 회귀 테스트를 추가한다. 재현되지 않은 버그를 수정했다고 말하지 않는다.
- 테스트는 별도 브라우저 세션에서 실행하고 온라인 랭킹/피드백 등 실제 외부 기록을 전송하지 않는다. 사용자 진행 상태와 설정을 함부로 덮어쓰지 않는다.
- 기존 게임들의 시작·피버 전환 회귀 검사도 실행한다. 검증 범위·실패·미검증 항목을 정확하게 보고한다.

현재 검사 예시(Playwright는 설치된 런타임 경로를 PLAYWRIGHT_MODULE로 지정):

- `node --test tests/*.test.mjs`
- `node tests/game-browser-smoke.mjs`
- 스택: `node tests/stack-continuous-browser.mjs` (90초 가상 진행 + 매 프레임 렌더)
- 스택: `node tests/stack-live-browser.mjs` (45초 실제 입력/루프 + 소리 ON/OFF)
- 슈팅: `node tests/shoot-browser.mjs` (60초 프레임별 검사 + 35초 실제 클릭, 피버/취소/결과/재시작)
- 게임별 추가 브라우저 검사는 `tests/`의 해당 파일을 확인한다.
