# 비주얼 1차 개편 — 장난감 아케이드 (2026-09-09)

## 2차 개편: 밝은 작은 세계 (같은 날, 사용자 참고 이미지 반영)

아래 1차 개편에서 메뉴 아이콘은 유지하고 게임 내부의 어두운 배경을 확장했다.
참고 작품의 캐릭터/이미지를 가져오지 않고 구름·씨앗 친구를 직접 그렸다.

- 캐치·콤보: 구름과 무지개 놀이터. 타이밍은 크림색 장난감 시계판.
- 체인: 청록빛 바다, 넓고 옅은 물결, 모래와 해초.
- 벌룬: 분홍빛 축제 하늘과 작은 천막.
- 스택·슈팅: 파스텔 마을/장난감 작업실. 레이싱은 밝은 잔디와 둥근 나무.
- 매칭·농장·보물찾기: 정원/해변 계열 배경. 농장 격자는 흙색으로 변경.
- 구슬/풍선의 표정은 숫자 아래, 로봇 눈은 숫자판 위에 동일하게 배치.
- 구름·장식 캐릭터는 시간값에 따라 천천히 움직인다. 게임 난수를 소비하지 않는다.
- 문제/점수는 기존 위치의 대비판으로 보호하고 부양 점수에는 어두운 외곽선을 추가.
- 레이싱은 전체 문제판을 쓰지 않는다. 기존 작은 칩을 유지해 먼 게이트를 가리지 않는다.
- 피버 진입 때 콤보 격자의 팝업 확대와 안내 문장이 겹치지 않도록 제목/안내 위치만 조정했다.
  레이싱 피버 안내는 먼 게이트 숫자를 가리지 않게 좌측에 두 줄로 배치했다.

새 모듈은 `src/art/arcadeWorld.js`. core/scenes/랭킹/설정/판정/속도는 이 2차 개편에서
수정하지 않는다. 보물찾기의 별도 개념 설명 화면도 기존 레이아웃을 유지한다.
자동 검증에는 모든 월드의 상태 복원·난수 미소비, 일반/피버 캡처, 4개 게임의 폰 캡처를 추가했다.

아래는 1차 개편 기록이다.

## 의도와 범위

기본 이모지와 단색 도형의 인상을 줄이고, 따뜻한 스티커·장난감 재질을 입힌다.
계산 속도, 점수, 피버 규칙, 오답 정책, 입력 영역은 바꾸지 않는다.
아이들이 실제로 더 재미있어하는지는 별도 플레이 관찰이 필요하다.

- 메뉴: 연습 게임 포함 12개 전용 스티커, 게임별 카드 색상.
- 콤보·매칭: 숫자/식 주변을 비워 둔 버튼 테두리 입체감.
- 캐치·체인·타이밍: 광택 구슬. 모든 후보의 스타일 동일.
- 슈팅: 숫자판을 가진 장난감 로봇과 발사기. 숫자가 얼굴보다 우선.
- 레이싱: 유리 반사·차체 도장·스트라이프. 도로/물리 로직 유지.
- 스택: 가장자리 포장 테이프. 가운데 숫자를 가리지 않는다.
- 벌룬: 풍선 음영, 굽은 실, 가장자리 하이라이트와 숫자 외곽선.
- 보물찾기: 깃발/공구상자 이모지 대신 해적 캐릭터·보물상자.
- 캐치·슈팅·체인·벌룬: 문제 영역 아래의 저대비 정적 배경 장식.
- 농장: 이번에는 메뉴 아이콘만 변경.

## 유지보수 / 랭킹 작업과의 접점

`src/art/toyArt.js`는 외부 파일을 불러오지 않는 Canvas 전용 그림 모듈이다.
좌표는 L 기반 크기를 전달하고, 그림 내부 비율은 그 크기에 비례한다.
게임 상태나 난수를 읽지 않으며, 답/함정 여부를 인자로 받지 않는다.
각 함수는 Canvas의 transform, alpha, 글자/선 스타일을 복원한다.

메뉴 변경은 `menuScene.js`의 import 및 카드/아이콘 그리기 부분뿐이다.
`drawGameCard(ctx, c.id, c, c.active)`와
`drawGameIcon(ctx, c.id, centerX, centerY, L.gu(2.25))`를 사용한다.
선택/최고점/업적/설정 이벤트는 유지한다. 랭킹 구현 시 이 두 그리기 호출을 보존하면 된다.
core, 랭킹·닉네임 파일, 데이터 저장 코드는 이번 시각 개편에서 수정하지 않았다.

## 검증

- `tests/art-browser-check.mjs`: 12개 아이콘 구분, 23개 렌더 호출의 Canvas 상태 복원·결정성,
  난수 미소비, 11개 게임 화면 및 데스크톱/폰 메뉴 캡처.
- `tests/game-browser-smoke.mjs`: 11개 게임 일반/피버 전환 및 농장 입력/결과 회귀.
- `tests/polish-browser-check.mjs`: 낙하물의 문제 영역 보호, 스택 진행 회귀.
- 기존 `game-polish.test.mjs`, `g11_farm.test.mjs`로 진행/점수/입력 규칙 확인.

새 브라우저 세션에서 자동 검증하며 플레이어의 기존 기록은 사용하지 않는다.
실제 학생 플레이, 저사양 휴대폰의 프레임 속도는 이 검증으로 대신하지 않는다.
