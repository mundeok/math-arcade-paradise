# 재미 검토와 추가 게임 명세 — 2026-09-08

> 후속 합의/구현: 피버의 쉬운 연타는 의도된 보상이므로 아래 초기 검토의 연타 차단 제안은 **채택하지 않음**. 일반 구간의 급격한 난이도 상승도 입증되지 않아 속도 공식은 유지한다. 이후 요청에 따라 문제 영역 보호·피버 문구 축소·버블 간격, 스택 정답 수 기반 배송/압축 회복, 종목별 무음 성공 연출을 구현했다. 최신 동작은 SPEC.md의 가독성·무음 손맛 보완 및 §4 스택 항목을 따른다. 아래는 당시 검토와 후보 명세 기록이다.

## 검토 범위와 작업 경계

SPEC.md, TODO.md, 게임 10개와 공통 점수·문제 생성·기록 코드를 대조했다. 아래 평가는 **코드에서 재현되는 문제와 설계 가설**이지, 초3 학생 대상 재미 검증 결과가 아니다.

검토 도중 Claude가 `src/core/problemGenerator.js`와 `SPEC.md`를 수정하는 것을 확인했다. 두 파일과 기존 게임·core·scenes는 이 작업에서 수정하지 않았다. 새 게임 파일, registry 등록, 개발 테스트와 이 문서만 추가/수정했다. 원본 SPEC 병합은 Claude의 난이도 패치 종료 후에 한다.

결론: 기존 10종이면 양은 충분하다. 지금 가장 큰 개선 여지는 **속도를 더 올리는 것보다, 읽고 판단한 행동이 보상받고 조작 실수는 덜 억울하게 만드는 것**이다. 새 게임은 기존 조작과 다른 배열 구성형 한 개만 체험용으로 추가했다.

## 먼저 고칠 것

### P1. 피버 격자는 계산 없이 한 자리 연타가 유리하다

- 근거: `src/games/g01_combo.js`의 `_judgeGrid` → `_refillGridBtn`, `g04_timing.js`의 `_judgeNum` → `_refillNum`.
- 맞든 틀리든 같은 위치가 즉시 새 숫자로 바뀌고, 새 숫자는 약 80% 확률로 배수다. `popT=0.15`는 시각 효과이지 재입력 차단이 아니다.
- 따라서 값을 보지 않고 같은 곳을 누르면 장기적으로 약 80%가 정답이고, 함정은 라이프·콤보 손실도 없다. 이는 확률/코드 분석이며 실제 아이의 행동 빈도를 측정한 것은 아니다.
- 권장: 선택한 칸은 잠깐 비우고 **다른 위치에서 보충**, 함정은 짧은 비활성 표시 후 교체. 정답 색상 사전 노출, 오답 라이프/콤보 차감, 별등급 감점은 넣지 않는다.
- 단, 위치 변경만으로 무작위 난타의 기대 이득까지 없어지지는 않는다. 배수 비중 80% + 무제한 무해 오답을 유지하면 이 한계는 남는다. 먼저 같은 자리 연타를 차단하고, 학생 테스트에서 무작위 훑기와 실제 판별을 비교해야 한다. 추가 보상에만 판별 연속성을 쓰는 안도 별도 검토한다.

### P1. 스택 압축 보상이 클리어를 늦춘다

- 근거: `src/games/g06_stack.js`의 `_catchCorrect`, `_judgeMulti`는 `_checkCompress()` 후 `stacked.length >= target`으로 완료를 판단한다.
- `7, 7, 7 → 21`은 +100점을 주지만 탑 높이는 3칸에서 1칸으로 줄어든다. 목표 4칸에서 좋은 일이 생겼는데 완료에는 두 번 더 받아야 한다.
- 권장: **웨이브 진행도(받은 정답 수)**와 **화면에 쌓인 블록 수**를 분리한다. 목표 4→5→6→7→8은 받은 정답 수로 판단하고, 압축은 높이 감소·곱셈 팝업·보너스로만 사용한다.
- 압축을 일부러 노리는 위험/보상 게임으로 유지하려면 대신 “높이 채우기”가 아닌 “가치 합계/배송량” 목표로 명시해야 한다. 둘 중 하나를 택해야 보상의 의미가 명확하다.

### P1. ‘화면 통과 시간’과 실제 조작 가능 시간이 다르다

- 근거: `src/games/g07_shoot.js`의 `_currentSpeed()`는 `L.H / sec`를 반환하지만, 조작 영역은 `topY=284`에서 `floorY=1024`까지다(기준 800×1280).
- 두 기준선 사이 이동만 계산하면 시작 4.2초 설정은 `740 / (1280 / 4.2) ≈ 2.43초`, 하한 2.2초 설정은 약 1.27초다. 스폰 시차, 반지름, 숫자를 읽기 시작하는 지점, 탄환 비행 시간은 별도로 고려해야 한다. 이 값을 그대로 학생의 실제 응답 시간이라고 부르면 안 된다.
- 권장: 숫자가 읽히기 시작하는 순간 → 최종 입력 마감까지의 **유효 판단 시간**을 정의한다. 속도는 실제 이동 거리로 계산하고, 조준과 탄환 도달 시간도 예산에 넣는다.
- 캐치·벌룬도 같은 기준으로 측정한다. 단순히 BASE_SEC 숫자만 늘리는 패치를 반복하지 않는 것이 좋다.

### P2. 공통 속도 가산은 콤보를 두 번 적용하고 게임마다 증가량이 다르다

- 근거: `scoreManager.speedFactor`에 콤보 항이 있고, 슈팅 등의 기존 속도 단계에도 콤보 항이 있다. 최종 속도는 양쪽이 곱해진다.
- 동일한 3,000점 가산 기준도 기본 10점 게임과 100점 게임, 피버 BOOST가 큰 게임에서 도달 속도가 다르다. 피버를 잘한 직후 일반 모드가 갑자기 어려워질 수 있다.
- 권장: 콤보 속도 항의 소유자를 한 곳으로 정하고, 장기 상승은 게임별 정규화 진행량/정답 수 또는 게임별 점수 단위로 비교한다. 원점수를 사용한다면 게임별 도달 시간 표를 먼저 만든다.
- 추가 확인: `g08_chain._bubbleSpeed()`는 200 논리px/s로 제한한 **뒤** 피버·교사 시간 배율을 적용한다. 따라서 200은 최종 상한이 아니다. “최종 상한”을 의도했다면 모든 배율 적용 후 클램프해야 한다.

### P2. 피버 학습 기록은 일반 계산 정답과 구분해야 한다

- 근거: 여러 multi 게임은 배수 선택 성공을 `dan × quotient` 계산 정답으로 기록한다. 그러나 배수 인식과 해당 곱셈을 독립적으로 계산하는 것은 같은 과제가 아니다.
- `g01`/`g04` 함정 경로는 `affectLevel:false`로 호출해 복습 큐도 건너뛴다. SPEC의 “피버 오답도 복습”과 다르다.
- 단순히 false를 true로 바꾸면 안 된다. 현재 함정 기록은 배수 선택을 나머지 있는 나눗셈 객체로 바꾼 것이라, 다음 게임이 처리할 수 없는 나머지 문제를 복습으로 받을 수 있다.
- 권장: 향후 core 수정 작업에서 `taskType`(계산/배수 판별/배열 구성/놓침), `fever`, 실제 제시 문항과 입력을 구분한다. 배수 판별 오답은 배수 판별로 복습한다. 이번 작업에서는 core 변경 없음.
- 기존 합의대로 **피버 오답의 라이프·콤보·별등급 면책은 유지**한다. 이것과 교사용 과제별 기록은 별개다.

## 게임별 재미 방향

아래는 우선 검증할 가설이며 기존 게임에는 적용하지 않았다.

| 게임 | 살릴 점 | 다음 개선/관찰 포인트 |
|---|---|---|
| g01 콤보 | 짧은 입력과 즉각 보상 | 같은 자리 연타 공략 제거가 1순위. 일반 QUICK과 피버 입력을 구분해 관찰 |
| g02 캐치 | 화면을 훑으며 잡는 감각 | 첫 등장부터 읽을 수 있는 시간 측정. 밀도와 속도를 동시에 올리지 않기 |
| g03 레이싱 | 3~4게이트 같은 레인으로 달리는 리듬 | 전환 예고를 아이가 실제로 알아보는지 확인. 숫자·예고·BOOST가 동시에 시선을 빼앗지 않기 |
| g04 타이밍 | 기다렸다 정확히 누르는 손맛 | 일반 바늘 리듬은 유지. 피버는 같은 자리 리필 공략과 구분되는 보충 방식 필요 |
| g05 매칭 | 연결 후 화면이 정리되는 만족감 | 웨이브 전환마다 조작 수가 너무 늘지 않는지 확인. 속도보다 정리 완료 보상을 강조 |
| g06 스택 | 축적·기울기 회복·압축 | 정답 누적 진행과 물리 높이를 분리해 압축을 순수 보상으로 만들기 |
| g07 슈팅 | 직접 조준하고 쏘는 능동성 | 숫자 인지→레인 이동→발사→명중의 전체 시간 예산부터 검증 |
| g08 체인 | 아무 배수나 가능 + 순서 보너스 | 순서가 끊긴 정상 배수는 성공 연출 유지, 체인 표시만 리셋. 최종 속도 상한 확인 |
| g09 벌룬 | 같은 값을 다른 식으로 발견 | 새 식 유형과 상승 속도를 같은 순간에 올리지 않기. 단계화 패치 후 실기 확인 |
| g10 보물 | 직접 분배하며 나머지를 이해 | 손으로 나누는 시간이 곧 학습. 반사신경 게임과 같은 속도 기준을 강제하지 않기 |

전체 게임을 같은 multi 피버로 통일할 필요는 없다. 쉬는 6초와 각 게임만의 조작 감각을 보존하는 쪽을 우선한다. 속도감은 이동·소리·수확/통과 연출로도 만들 수 있다.

## SPEC 보완 제안 — Claude 패치 후 병합

1. **§2.6 / §7.6 피버 표를 현재 구현으로 갱신.** 기존 10종 기준 multi는 g01·g02·g03·g04·g06·g07·g09, easy는 g05·g08·g10이다. g03은 정답 1/3, 다른 multi는 대략 80%라는 차이도 명시한다. 새 농장은 easy 체험 게임이다.
2. **기본 규칙과 게임별 예외를 분리.** 기본 속도 1.35배, 캐치는 피버 속도 1.0배/개수 7개, 레이싱은 시각 흐름 2배/게이트 1초 등은 한 문장으로 묶을 수 없다. 실제 설정/계산 결과를 기준으로 표를 유지한다.
3. **§2.1 축 B와 §3.4의 충돌 해소.** ‘콤보로만 계산’은 점수 항 도입 이후 사실과 다르다. “수학 level을 물리 속도 계산에 쓰지 않는다”와 “콤보·세션 진행을 쓴다”를 구분한다.
4. **§2.5 / §3.4에 유효 판단 시간 정의 추가.** 전체 캔버스 높이가 아니라 읽기 가능 시점→입력 마감, 최종 배율 뒤 안전 클램프, 조작별 이동/조준 시간을 명시한다.
5. **§2.3 / §3.7에 과제 종류와 오답 종류 명시.** 수학 오답, 순서 보너스 끊김, 물리적 놓침, 피버 함정을 같은 통계로 단정하지 않는다. 현재 기록 포맷의 한계를 먼저 적는다.
6. **§4 스택 완료 조건 정리.** 압축과 목표 높이의 충돌을 위 두 설계안 중 하나로 해결한다.
7. **§4 / 메뉴 확장 제한.** 현재 메뉴는 더미 포함 12칸이 마지막 줄까지 차므로 이번 1종 추가까지만 화면 안에 들어간다. 추가 종목은 페이지/카테고리 또는 더미 숨김 설계가 먼저다.
8. **재미의 완료 기준 추가.** 프레임 오류 없음은 재미 검증이 아니다. 학생이 규칙을 설명하는지, 자발적으로 한 판 더 고르는지, ‘생각은 맞았는데 손이 늦었다’고 느끼는지 별도로 관찰한다.

수학 Lv 표와 새 오답지 생성 정책은 Claude가 수정 중이므로 이 검토서에서 대체하지 않는다.

## 추가 명세: 11. 🌱 곱셈 농장 — 배열 구성형 체험 게임

구현: `src/games/g11_farm.js`, 메뉴 등록: `src/games/registry.js`.

### 추가 이유와 범위

기존에는 선택·타이밍·받기·이동·분배가 있지만, **같은 수 묶음을 직사각형으로 직접 구성하는 조작**은 없다. 또 하나의 탭 반사신경 게임 대신 동수누가와 곱셈/나눗셈의 관계를 눈으로 보여주는 후보를 추가했다. 정식 11번째 필수 게임으로 확정하기 전 학생 테스트가 필요하다. 면적 단위나 인수분해 용어는 가르치지 않는다.

### 규칙

- id `g11_farm`, `opMode:'mixed'`, `maxLevel:2`, `blankRatio:0`, `fever:{type:'easy'}`.
- 9×9 밭 위 드래그로 직사각형 크기를 정한다. 배열의 시작점은 왼쪽 위로 고정한다.
- **곱셈:** “24칸의 밭!” → 3×8, 4×6 등 목표 칸 수와 같은 배열이면 모두 정답. 드래그 중 줄 수와 한 줄 칸 수만 보여주고 합계는 숨긴다.
- **나눗셈:** “24÷4=?” → 4줄 고정, 한 줄에 6칸이 되도록 좌우로 늘린다.
- 드래그 후 큰 **수확!** 버튼으로 제출. 드래그 종료만으로는 정오답 판정하지 않는다. 방향키로 크기 조절, Enter/Space로 제출도 가능하다.
- 셀은 개별 탭 버튼 81개가 아니라 하나의 연속 드래그 영역이다. 제출 버튼은 L.minTouch 이상이다. 좁은 폰에서 원하는 크기를 정확히 고르는지는 실기 확인 대상이다.
- 정답은 즉시 다음 밭으로 진행. 새싹 배열과 완료한 밭의 작은 실루엣으로 수확 흔적을 보여준다. 식과 점수는 하단 진행 표시를 잠깐 대체해 다음 문제/밭을 가리지 않는다.
- 기본 점수 `100 + 콤보×5`, 4초×교사 timeScale 이내 +20. 피버 정답 배수는 core 적용. 4개 완성마다 별도 고정 +100. **16개 완성하면 결과 화면**으로 끝난다.
- 타임아웃/속도 상승은 없다. 빠른 풀이에는 보너스만 주고, 느린 풀이를 벌주지 않는다. 총 4회 수확이라는 짧은 완주 목표로 리듬을 만든다.
- 오답은 기존 공통 라이프/피드백/복습 큐를 사용한다. 곱셈 피드백은 가능한 배열 중 원래 생성된 한 가지를 보여준다.

### 출제·피버·기록

- `nextProblem()`으로 출제해 공통 복습/중복 방지 경로를 유지한다. mixed일 때 성공한 밭마다 요청 연산을 번갈아 바꾸며, 교사 연산 고정은 공통 생성기가 우선한다.
- 배열 표현이 가능한 구구단/구구단 역산 범위(Lv1~2)로 상한을 둔다. 피버 중에는 Lv1을 요청한다. 수학 level을 물리 속도에 사용하지 않는다.
- 단 선택의 레벨별 교집합/폴백 정책은 공통 생성기를 그대로 따른다. 현재 `activeDans()`는 교집합이 비면 레벨 기본 단으로 되돌아가므로, ‘선택한 단만 절대 출제’가 보장된다고 주장하지 않는다. 이 문제는 공통 정책 검토 대상이다.
- 피버 진입/종료 시 미제출 배열·드래그·버튼 누름 예약만 정리하고, 완료한 밭은 유지한다. 오답 무적과 복습은 공통 처리, 종료 획득 점수는 게임 내 실제 점수 차이로 표시한다.
- 기록에는 공통 생성기의 기준 문제가 남는다. **같은 넓이의 다른 배열을 맞힌 기록을 원래 곱셈식의 암기 숙달로 해석하면 안 된다.** 배열 구성 과제임을 고려해야 하며, 정확한 교사 리포트 분리는 위 taskType 확장 제안에 포함한다.
- 자체 시간제한이 없으므로 니어미스 보너스를 억지로 만들지 않는다. §2.6의 전 게임 공통 요구에 대한 명시적 체험 게임 예외 제안이다.

## 검증 및 학생 테스트 권장 순서

자동 검증:

- `node --test tests/g11_farm.test.mjs`: 7개 테스트. 연산/Lv1~2 문제 600개, 대체 배열, 나눗셈 고정 줄, 드래그와 제출 분리, 복습, 피버 전환, 16개 완주, 메뉴 배치.
- `tests/game-browser-smoke.mjs`: Playwright 개발 검증. 기존 10종+농장 초기화/일반 렌더/피버 진입·종료 렌더, 농장 실제 마우스 드래그·제출, 실제 core 오답/무적/결과 화면, 375×667 화면 확인. 장시간 자동 플레이나 실제 터치 기기 검증을 대체하지 않는다.
- 개발 테스트에만 Playwright를 사용한다. 게임 런타임 라이브러리·외부 자산은 추가하지 않는다.

학생 관찰 제안(확정 합격 기준이 아님):

1. 기존 레이싱/캐치/슈팅에서 첫 3문제를 설명 없이 시작할 수 있는지 본다.
2. 피버에서 숫자를 읽는지, 같은 자리를 누르는지, 무작위로 훑는지 구분한다.
3. 압축이 일어난 뒤 “좋다”와 “왜 아직 안 끝나?” 중 어떤 반응인지 듣는다.
4. 농장은 24칸을 두 가지 모양으로 만들 수 있는지, 나눗셈에서 줄 수 고정을 이해하는지 본다. 16밭 완주가 지루하면 개수부터 줄인다.
5. 마지막에 “가장 재미있었던 게임”만 묻지 말고 **한 판 더 할 게임을 직접 고르게** 한다. 틀려서 싫은 것인지 조작 때문에 억울한 것인지 따로 묻는다.

일일 접속 보상·연속 출석·무한 진행으로 붙잡기보다, 짧은 완주와 스스로 다시 고르는 재미를 우선한다.
