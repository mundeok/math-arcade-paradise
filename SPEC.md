# 곱셈나눗셈 아케이드 천국 — 개발 명세서 (SPEC)

> **이 문서는 프로젝트의 단일 진실 공급원(Single Source of Truth)이다.**
> 모든 Phase 작업은 이 문서를 먼저 읽고 시작한다.
> 이 문서와 충돌하는 코드가 발견되면 **이 문서가 우선**이다.
> 이 문서를 수정해야 할 상황이 생기면, 코드를 고치기 전에 사용자에게 먼저 보고한다.

---

## 0. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 제목 | 곱셈나눗셈 아케이드 천국 (Math Arcade Paradise) |
| 대상 | 초등학교 3학년 |
| 교과 | 수학 — 곱셈, 나눗셈 |
| 콘셉트 | 컴투스 '미니게임천국' / '액션퍼즐패밀리' 감성의 초고속 캐주얼 아케이드 10종 모음 |
| 사용 환경 | 교실 (삼성 갤럭시 탭, 학생 1인 1대, 동시 최대 30대) |
| 최종 산출물 | 외부 리소스 0개인 단일 HTML 파일 (`dist/index.html`) |
| 배포 | GitHub → Vercel 정적 호스팅 |

### 설계 철학
1. **재미가 먼저다.** 학습 게임이 아니라 "수학이 소재인 아케이드 게임"을 만든다. 재미없으면 학습도 없다.
2. **틀리는 것이 벌이 아니다.** 오답은 즉시 정답을 알려주고 다시 만나게 하는 기회다.
3. **교실은 통제 가능해야 한다.** 교사가 범위·속도·소리를 제어할 수 있어야 한다.
4. **3초 안에 이해된다.** 튜토리얼은 그림 한 장 + 문장 하나.

---

## 1. 기술 사양 (전 Phase 공통, 예외 없음)

### 1.1 실행 환경
- **HTML5 Canvas 2D만 사용.** WebGL 금지.
- **외부 리소스 0개.** CDN, 폰트 파일, 이미지 파일, 오디오 파일 전부 금지.
  - 아이콘/캐릭터는 Canvas 도형 드로잉 또는 이모지 문자로 표현
  - 폰트는 시스템 폰트 스택만: `-apple-system, "Noto Sans KR", "Malgun Gothic", sans-serif`
- **효과음은 Web Audio API 신디사이저로 코드 생성.** 오디오 파일 금지.
- `requestAnimationFrame` + **delta time 기반** 업데이트. 프레임 수에 의존하는 로직 금지.

### 1.2 화면 규격 ⚠️ 중요
- **기준 해상도: 800 × 1280 (갤럭시 탭 세로 모드)**
- 가로 모드 진입 시: "기기를 세로로 돌려주세요" 안내 화면 표시 (게임 일시정지)
- 캔버스는 논리 좌표 **800×1280 고정**으로 그리고, CSS로 화면에 맞춰 스케일링한다.
  ```
  논리 캔버스: 800 x 1280 (모든 게임 좌표 계산의 기준)
  실제 렌더: devicePixelRatio 반영해서 backing store 확대
  화면 표시: object-fit contain 방식으로 letterbox
  ```
- **터치 입력은 반드시 화면 좌표 → 논리 좌표로 변환**해서 처리한다. (스케일 보정 함수 공용화)
  - ⚠️ **좌표 변환은 `canvas.getBoundingClientRect()` 기준만 사용한다.** 캔버스 요소가 이미
    레터박스 위치(`style.left/top = offsetX/offsetY`)에 놓이므로, `rect.left/top`을 뺀 뒤
    **offset을 추가로 빼면 이중 차감**이 되어 좌표가 밀린다. (세로 비율이 800:1280과 다른
    PC 창에서만 드러나 탭 테스트를 통과할 수 있으니 특히 주의 — 실제 회귀 발생 이력 있음.)
- 터치 타겟 최소 크기: **논리 좌표 기준 96 × 96px**
- 문제 텍스트 크기: 최소 **80px** (논리 좌표)
- 안전 여백: 상하좌우 24px

#### 반응형 대응 원칙 — Phase 1부터 전 게임 준수 ⚠️
최종 목표는 **태블릿·크롬북(교실)과 스마트폰(가정)을 모두 지원**하는 것이다.
Phase 6에서 논리 캔버스 크기가 화면 비율에 따라 달라질 수 있으므로, **지금부터** 모든 게임은 아래를 지킨다.

1. **레이아웃 좌표는 논리 캔버스 크기(W, H)에 대한 비율로 계산한다.**
   - 금지: `const BTN_X = 40`
   - 권장: `const BTN_X = W * 0.05`
   - 버튼 위치, `FLOOR_Y`, 게이지 위치 등 **모든 배치값**에 적용한다.
2. **터치 타겟은 실제 화면에서 최소 44 CSS px**가 되도록 보장한다.
   폰에서 축소될 때 손가락으로 누를 수 없으면 안 된다.
3. **폰트 크기도 비율 기반**으로 한다. 문제 텍스트는 **화면 높이의 6% 이상**.
4. 가로가 긴 화면(크롬북 등)에서 **레터박스가 크게 생기는 것은 정상**이다.
5. **화면 비율 프로필** (Phase 6에서 구현, 지금은 문서화만)
   - A. `1:1.3 ~ 1:1.8` (태블릿·크롬북) → 논리 캔버스 **800×1280**
   - B. `1:1.8 ~ 1:2.4` (폰 세로) → **폭 기준 스케일**, 위아래 여백 허용
   - C. 그 외 (정사각형·가로) → **안내 화면 표시, 게임 진입 차단**
   - Z플립 커버 화면(약 `1:1.06`) 등 극단적 비율은 **C로 처리하며 지원하지 않는다.**
6. 화면이 작아 터치 타겟 44px를 만족할 수 없으면, 게임은 **동시 등장 요소 수를 줄여**
   각 요소를 크게 만드는 방향으로 대응한다. (Phase 6)

> **구현 헬퍼**: 위 원칙 1·2·3은 `core/layout.js`의 `L` 헬퍼로 구현한다
> (`L.x/y/w/h/font/safe/minTouch`). 신규 게임은 처음부터 `L`로 작성한다.
> ⚠️ **기존 `g01_combo.js`, `g02_catch.js` 는 헬퍼 도입 이전에 작성되어 절대 좌표를 사용한다.
> Phase 6에서 `L` 헬퍼로 치환한다** (TODO.md 참조).

### 1.3 개발 중 파일 구조 (단일 파일은 최종 빌드에서만)
```
/
├─ SPEC.md              ← 이 문서
├─ src/
│  ├─ core/
│  │  ├─ engine.js          게임 루프, 상태 머신, 캔버스/스케일 관리
│  │  ├─ input.js           터치·마우스·키보드 통합 입력
│  │  ├─ layout.js          반응형 좌표/크기/폰트 헬퍼 (L: 비율→논리좌표)
│  │  ├─ problemGenerator.js  문제 생성 + 오답지 생성 + 복습 큐
│  │  ├─ scoreManager.js    점수·콤보·라이프·별등급·업적
│  │  ├─ soundManager.js    Web Audio 신디사이저
│  │  ├─ particle.js        파티클 시스템
│  │  ├─ storage.js         localStorage 래퍼
│  │  ├─ ui.js              공용 UI 컴포넌트 (버튼, HUD, 텍스트 연출)
│  │  ├─ session.js         세션 기록 (문제/정답/반응시간)
│  │  └─ mathText.js        문제 → 식 문자열 변환 (오답 표시·결과 목록 공용)
│  ├─ scenes/
│  │  ├─ menuScene.js
│  │  ├─ tutorialScene.js
│  │  ├─ resultScene.js
│  │  ├─ settingsScene.js   교사 설정
│  │  └─ reportScene.js     학습 리포트
│  ├─ games/
│  │  ├─ _dummy.js          Phase 0 검증용
│  │  ├─ g01_combo.js
│  │  ├─ g02_catch.js
│  │  ├─ ...                (Phase별로 추가)
│  │  └─ registry.js        게임 목록 등록
│  └─ main.js
├─ build.js             모든 src를 단일 HTML로 인라인 병합
├─ index.html           개발용 (module script 로드)
└─ dist/index.html      최종 산출물
```

**규칙: 새 게임 추가 시 `src/games/` 에 파일을 새로 만들고 `registry.js`에만 한 줄 등록한다. 기존 게임 파일과 core 파일은 절대 수정하지 않는다.** core 수정이 필요하면 반드시 사용자에게 먼저 물어본다.

### 1.4 코드 규칙
- ES6 모듈, `class` 기반
- 전역 변수 금지 (엔진 인스턴스 하나만 전달)
- localStorage 키 네임스페이스: `mathArcade.*`
- 주석은 한글로, "왜 이렇게 했는지"를 적는다

---

## 2. 교육 설계 (최우선 준수)

### 2.1 난이도 2축 분리 ⚠️ 절대 규칙

두 축은 **완전히 독립적으로 동작**한다. 서로 참조하지 않는다.

#### 축 A — 수학 난이도 (`level`, 1~5)
- `ProblemGenerator`만 사용한다. 게임 로직은 이 값을 읽지 않는다.
- 상향: 정답 콤보가 **8의 배수에 도달할 때** +1 (최대 5)
- 하향: **2연속 오답** 시 -1 (최소 1)
- 교사 설정에서 '레벨 고정 모드' ON이면 자동 조정 비활성

**혼합(mixed) 모드 사다리** (교사 설정 '연산 종류 = 혼합', 기본값)

| Lv | 내용 | 예시 |
|---|---|---|
| 1 | 곱셈구구 2~5단 | 3×4, 5×7 |
| 2 | 곱셈구구 6~9단 | 7×8, 9×6 |
| 3 | 나눗셈 (구구단 역산, 나누어떨어짐) + 빈칸 역산(□×, ÷□ 모두) | 42÷7, □×7=42, 48÷□=6 |
| 4 | 두 자리 × 한 자리 (올림 포함) / 두 자리 ÷ 한 자리 (나누어떨어짐) | 23×3, 47×2, 96÷4 |
| 5 | 세 자리 × 한 자리 / 나머지 있는 나눗셈 | 213×3, 17÷5=3…2, 74÷9=8…2 |

#### 연산 모드 전용 레벨 사다리 ⚠️
교사 설정 '연산 종류'가 **곱셈만 / 나눗셈만**일 때는 각각 아래의 독립된 5단계를 쓴다.
혼합 모드와 달리 한 연산만 5단계로 세분한다.

**곱셈만(multiply) 모드**

| Lv | 내용 | 예시 |
|---|---|---|
| 1 | 곱셈구구 2~5단 | 3×4 |
| 2 | 곱셈구구 6~9단 | 7×8 |
| 3 | 두 자리 × 한 자리 (올림 없음) | 21×3 |
| 4 | 두 자리 × 한 자리 (올림 있음) | 47×2 |
| 5 | 세 자리 × 한 자리 | 213×3 |

**나눗셈만(divide) 모드**

| Lv | 내용 | 예시 |
|---|---|---|
| 1 | 나눗셈 2~5단 역산 (나누어떨어짐) | 12÷4 |
| 2 | 나눗셈 6~9단 역산 (나누어떨어짐) | 56÷7 |
| 3 | 빈칸 역산 (**÷□ 형만**, □×형 미출제) | 48÷□=6 |
| 4 | 두 자리 ÷ 한 자리 (나누어떨어짐) | 96÷4 |
| 5 | 나머지 있는 나눗셈 | 74÷9=8…2 |

#### 빈칸형(□) 출제 비율 — 게임 유형별 `blankRatio` ⚠️
빈칸형(□×7=42, 48÷□=6)은 반사신경형에겐 너무 어렵다. 게임 객체의 `blankRatio` 필드로 조절한다.
`nextProblem({ maxLevel, blankRatio })`가 이 값을 참조한다. (빈칸이 가능한 레벨에서 빈칸이 뽑힐 확률)

| 유형 | 게임 | `blankRatio` |
|---|---|---|
| 반사신경형 | 2️⃣, 7️⃣, 9️⃣ | **0** (빈칸 미출제) |
| 판단형 | 1️⃣, 4️⃣, 6️⃣, 8️⃣ | **0.25** |
| 사고형 | 3️⃣, 5️⃣, 🔟 | **0.4** |

- 기본값은 0.25. 더미 게임도 0.25.

#### 축 B — 게임 난이도 (속도·개수·혼동값)
- 각 게임이 **현재 콤보 수**를 보고 자체적으로 계산한다.
- 낙하 속도, 동시 등장 개수, 오답 근접도 등이 여기 속한다.
- 각 게임 명세의 "게임 난이도 곡선" 항목 참조.

#### 게임별 출제 레벨 상한 ⚠️
반사신경형 게임에 두 자리 곱셈을 내면 절대 못 푼다. 반드시 지킨다.

| 유형 | 게임 | 출제 상한 |
|---|---|---|
| 반사신경형 | 2️⃣ 떨어지는 캐치, 7️⃣ 슈팅 계산, 9️⃣ 벌룬 팝 | **Lv 1~3** |
| 판단형 | 1️⃣ 콤보 챌린지, 4️⃣ 타이밍 퍼즐, 6️⃣ 스택 빌더, 8️⃣ 배수 체인 | **Lv 1~4** |
| 사고형 | 3️⃣ 레이싱, 5️⃣ 숫자 매칭, 🔟 나머지 보물찾기 | **Lv 1~5** |

### 2.2 오답지(방해 숫자) 생성 규칙 — 반드시 코드로 구현

정답 하나에 대해 아래 전략 중 무작위로 골라 오답을 만든다.

```
1. 인접 구구단 값      6×7=42 → 36(6×6), 48(6×8), 49(7×7), 35(5×7)
2. 자릿수 뒤집기        42 → 24
3. 덧셈 혼동 값         6×7 → 13 (6+7)   ※ Lv1~2에서만 사용
4. ±1, ±2 근접값        42 → 41, 43
5. ±10 근접값           42 → 32, 52
6. 나눗셈 몫 오차       42÷7=6 → 5, 7, 8
```

**제약:**
- 오답은 정답과 절대 중복 금지, 오답끼리도 중복 금지
- 모든 오답은 **양의 정수**
- 콤보가 낮을 때는 정답과 멀리(전략 3,5), 높을 때는 가깝게(전략 1,4) — 이것도 축 B에 해당
- 오답 후보가 부족하면 정답 ±무작위(1~15)로 채운다

### 2.3 오답 피드백 (교육 게임의 핵심)
- 오답 또는 시간초과 시 → **정답을 화면 중앙에 큰 글씨(120px)로 1.2초간 표시**
  - 표시 형식: `6 × 7 = 42` (식 전체)
  - 나머지 있는 나눗셈은: `17 ÷ 5 = 3 … 2` 와 함께 `17 = 5 × 3 + 2`
- 1.2초 동안 게임은 **완전 정지** (타이머, 물체 이동 전부 멈춤)
- 틀린 문제는 **복습 큐**에 저장 → **2~3문제 뒤 같은 세션에서 재출제**
- 복습 큐 문제는 정답 맞히면 큐에서 제거, 또 틀리면 큐 유지
- 결과 화면에 "틀린 문제 다시보기" 목록 표시

### 2.4 문제 중복 방지
- 최근 5문제와 동일한 문제는 재출제 금지 (복습 큐 문제는 예외)

### 2.5 접근성 / 정서 안전 ⚠️
- ❌ **금지: 화면을 어둡게 하거나 색을 반전시키는 난이도 메커닉**
- ❌ **금지: 위협적 효과음** (비명, 비웃음, 공포음). 실패음은 짧고 중립적인 저음(220Hz 이하, 0.2초 이내)
- ❌ **금지: 색만으로 정오답 구분.** 반드시 `색 + 아이콘(⭕/❌) + 모양 변화` 3중으로 표현 (적록색약 대응)
- ❌ **금지: 초3이 모르는 어휘.** 모든 UI 한글, 쉬운 말로
- ❌ **금지: 학생을 부정적으로 평가하는 문구** ("실패", "틀렸어요" 대신 "아쉬워요", "다시 해보자")
- 깜빡임(flash)은 초당 3회 이하 (광과민성 발작 예방)

---

## 3. 통합 시스템

### 3.1 상태 머신
```
MENU ─→ TUTORIAL ─→ PLAYING ─┬→ PAUSED ─→ PLAYING
  ↑                          └→ RESULT ─┬→ MENU
  │                                     ├→ PLAYING (다시하기)
  └─────────────────────────────────────┘  └→ TUTORIAL (다음 게임)
  
MENU ─→ SETTINGS (교사 설정)
MENU ─→ REPORT   (학습 리포트)
MENU ─→ ORIENTATION_WARNING (가로 모드 시)
```

### 3.2 라이프
- **게임별 독립 3개** (게임 간 공유하지 않음)
- **회복: 콤보가 10 / 20 / 30에 최초 도달하는 순간 각 1회, 게임당 최대 2회, 상한 5개**
  - 반드시 `recoveredMilestones = Set()` 같은 플래그로 관리 → 무한 회복 방지
  - 콤보가 리셋되어 다시 10에 도달해도 재회복 없음
- 라이프 0 → RESULT

### 3.3 콤보
- 정답 시 +1, 오답/시간초과 시 0으로 리셋
- 게임별 독립. 세션 최고 콤보를 별도 기록
- 콤보 연출 (공용 텍스트 오버레이):
  - 5 → `GREAT!`
  - 10 → `PERFECT!` + 파티클
  - 20 → `AMAZING!` + 파티클 대량
  - 30 → `LEGEND!` + 화면 전체 연출
  - ※ 게임별 고유 문구가 있으면 그것을 우선 사용

### 3.4 점수
- 기본 공식: `획득점수 = 게임별 기본점 + (현재콤보 × 게임별 계수)`
- 게임별 기본점/계수는 각 게임 명세 참조

### 3.5 별 등급 (정답률 기준)
| 등급 | 정답률 |
|---|---|
| ⭐ | 50% 미만 |
| ⭐⭐ | 50 ~ 69% |
| ⭐⭐⭐ | 70 ~ 84% |
| ⭐⭐⭐⭐ | 85 ~ 94% |
| ⭐⭐⭐⭐⭐ | 95% 이상 |

### 3.6 업적 (게임별 3단계)
- 🥉 초급: 최고 콤보 5 달성
- 🥈 중급: 최고 콤보 15 달성
- 🥇 고급: 최고 콤보 30 달성
- localStorage에 영구 저장, 메뉴에서 게임 버튼에 뱃지 표시

### 3.7 세션 기록 구조
복습 큐와 학습 리포트가 이 배열을 공유한다.
```js
{
  gameId: 'g01_combo',
  question: { text: '6 × 7', a: 6, b: 7, op: '×', answer: 42, remainder: null, level: 2 },
  userAnswer: 36,
  correct: false,
  responseMs: 2140,
  timestamp: 1735000000000
}
```

#### 학습 리포트 단(dan) 집계 규칙
`mathArcade.report`의 세션 기록으로 단별 정답률을 낼 때 아래 규칙을 따른다.

| 문제 형태 | 집계 대상 | 예시 |
|---|---|---|
| 한 자리 × 한 자리 | **양쪽 단 모두** (+1씩) | 6×7 → 6단, 7단 |
| 나눗셈 (나머지 없음) | **제수의 단** | 42÷7 → 7단 |
| 두 자리 이상 × 한 자리 | **한 자리 쪽 수의 단** | 23×3 → 3단 |
| 세 자리 × 한 자리 | 단 제외, **'Lv5'** 항목으로 별도 | 213×3 → Lv5 |
| 나머지 있는 나눗셈 | 단 제외, **'Lv5'** 항목으로 별도 | 74÷9=8…2 → Lv5 |

### 3.8 localStorage 스키마
```
mathArcade.highScores.{gameId}   → [{score, combo, accuracy, date}, ...] 최대 5개
mathArcade.achievements          → { 'g01_combo': ['bronze','silver'], ... }
mathArcade.settings              → 교사 설정 객체
mathArcade.report                → 최근 200개 세션 기록 (단별 정답률 집계용)
```

### 3.9 교사 설정 (⚙️ 메뉴 우측 상단, **1.5초 길게 눌러** 진입 — 학생 오조작 방지)
| 항목 | 옵션 | 기본값 |
|---|---|---|
| 연산 종류 | 곱셈만 / 나눗셈만 / 혼합 | 혼합 |
| 단(dan) 선택 | 2~9단 체크박스 | 전체 |
| 레벨 고정 | ON(고정 레벨 선택) / OFF(자동) | OFF |
| 제한시간 배율 | 0.8× / 1.0× / 1.5× | 1.0× |
| **효과음** | **ON / OFF** | **OFF** ⚠️ |
| 배경음악 | ON / OFF | OFF |
| 학습 리포트 초기화 | 버튼 | — |

> ⚠️ **효과음 기본 OFF는 필수다.** 30명이 동시에 사용하는 교실에서 기본 ON이면 수업이 불가능하다.

> ⚠️ **'나눗셈만' 모드는 × 기호가 들어간 문제(□×7=42 형태)를 출제하지 않는다.** 빈칸은 `48÷□=6`(÷□) 형태만 쓴다. `□×7=42`(× 기호)는 **혼합 모드에서만** 나온다. 연산 모드별 레벨 사다리는 §2.1 참조.

### 3.10 기타 공통 요구사항
- **일시정지**: 게임 중 항상 ⏸ 버튼 노출. 안드로이드 뒤로가기 키 → 일시정지
- **이탈 방지**: 게임 중 새로고침/닫기 시 `beforeunload` 경고
- **파티클 상한**: 동시 200개. 초과 시 오래된 것부터 제거 (태블릿 프레임 드랍 방지)
- **AudioContext**: 첫 사용자 터치 후 `resume()`. 그 전까지 무음은 정상 동작 (에러 아님)
- **리사이즈**: `resize` / `orientationchange` 이벤트에서 캔버스 재계산

---

## 4. 게임 명세 (10종)

> 각 게임은 아래 인터페이스를 반드시 구현한다.
> ```js
> {
>   id: 'g01_combo',
>   name: '콤보 챌린지',
>   emoji: '⚡',
>   category: '선택형',
>   maxLevel: 4,                    // 출제 레벨 상한
>   tutorial: { text: '...', draw(ctx) {...} },
>   init(engine),
>   update(dt),
>   render(ctx),
>   onTouch(x, y, phase),          // phase: 'start' | 'move' | 'end'  (논리 좌표)
>   onKey(e),
>   destroy()
> }
> ```

---

### 1️⃣ ⚡ 콤보 챌린지 — 선택형 · 기초
**Phase 1 / 출제 상한 Lv4**

- 화면 상단에 문제, 하단에 **선택지 4개** (2×2 그리드, 각 버튼 340×160)
- 제한시간 기본 **5초** → 콤보 5마다 0.3초 단축, **최소 2.5초**
- 남은 시간은 화면 상단 가로 게이지바로 표시
- 정답: **100점 + (콤보 × 10)**
- 오답/시간초과: 콤보 리셋 + 라이프 -1 + 정답 1.2초 표시
- 게임 난이도 곡선: 제한시간 단축, 오답지 근접도 상승

**튜토리얼**: "문제의 답을 찾아 눌러봐! 빠를수록 점수가 올라가!"

---

### 2️⃣ 🎪 떨어지는 캐치 — 반사신경
**Phase 1 / 출제 상한 Lv3**

- 상단에 문제 고정, 화면 위에서 숫자가 떨어짐. **정답만 터치**
- 오답 **터치**: 라이프 -1 + 콤보 리셋 + **정답 1.2초 표시(화면 정지)** — 이해 오류이므로 확실히 짚어준다
- **정답을 놓쳐서 바닥에 닿으면('놓침'): 콤보만 리셋 (라이프는 유지)** — 반사신경 게임이라 관대하게
  - ⚠️ **놓침은 오답 '터치'와 다르게 처리한다** (반응 속도 문제이지 이해 부족이 아님):
    - **화면을 멈추지 않는다** (1.2초 정답표시 오버레이 호출 금지). 즉시 다음 낙하로 진행
    - **무음** (실패음 없음). 시각 연출만: 바닥에서 원이 터지는 파티클 + "앗!" 텍스트 0.4초
    - **수학 난이도(레벨) 하향·복습 큐 등록 없음** (`affectLevel:false`)
    - **세션에는 오답(`missed:true`)으로 기록** → 결과 화면 '틀린 문제 다시보기'에는 포함(놓침으로 구분 가능)
    - 구현: `engine.answerWrong(problem, null, { loseLife:false, freeze:false, affectLevel:false, missed:true })`
- 낙하 속도: 시작 **1.5초/화면** → 콤보 5마다 ×1.2(빨라짐), **상한 0.6초/화면(클램프)** · 교사 제한시간 배율 반영
- 동시 등장: 콤보 **0~14 → 정답1+오답2 / 15~29 → 정답1+오답3 / 30+ → 정답1+오답4**
- 정답: **50점 + (콤보 × 5)**
- 콤보 연출: 7 `NICE!`, 15 `AWESOME!` (그 외 5/10/20/30은 core 기본 연출)

**튜토리얼**: "떨어지는 숫자 중에서 답을 찾아 콕 눌러!"

---

### 3️⃣ 🚀 레이싱 계산 — 경쟁형
**Phase 5 / 출제 상한 Lv5**

- 내 차 vs AI 차, **총 30칸** 트랙 (세로 화면이므로 위로 올라가는 형태)
- 정답 → 내 차 1칸 전진 / 오답 → AI 1칸 전진 + 정답 표시
- AI는 **일정 시간마다 자동 전진** (경주마다 간격 단축)
- ⚠️ **라이프 규칙 예외**: 이 게임의 라이프는 "남은 도전 기회 3회"다.
  - **오답으로는 라이프가 깎이지 않는다.** AI만 전진한다.
  - **경주에서 패배할 때만 라이프 -1**
  - 라이프 0 (= 3패) → RESULT
- 승리: **500점**, 연승 보너스 `500 × 연승수`
- 패배 직후 다음 경주는 AI 전진 간격 +30% (동정 시스템)
- 사운드: 정답 시 엔진 부스터음, AI 추월 시 짧은 경적음 — **비웃음·조롱 사운드 절대 금지**

**튜토리얼**: "답을 맞히면 내 차가 앞으로 나가! 먼저 결승선에 도착하자!"

---

### 4️⃣ 🎯 타이밍 퍼즐 — 정밀형
**Phase 3 / 출제 상한 Lv4**

- 화면 중앙에 큰 원. 원 둘레에 **숫자 4~6개 배치** (정답 1개 + 오답들)
- 바늘이 회전 → **바늘이 '정답 숫자' 영역 위를 지날 때 탭**
- 판정: `PERFECT`(정답 중심 ±5도) 100점 / `GOOD`(±15도) 60점 / `OK`(±30도) 30점
- **오답 숫자 위에서 탭**: 라이프 -1 + 정답 표시
- **3회전 안에 못 맞히면**: 시간초과 처리 (라이프 -1) ← 무한 대기 방지
- 회전 속도: 3.0초/회전 → 콤보에 따라 **1.2초/회전**까지
- 남은 회전 수를 원 중앙에 표시

**튜토리얼**: "바늘이 정답 위를 지나갈 때 화면을 눌러!"

---

### 5️⃣ 🧩 숫자 매칭 — 전략형
**Phase 4 / 출제 상한 Lv5**

- 좌측: 문제 카드 (`4×6`, `56÷7`, `□×3=27` 빈칸형 혼합)
- 우측: 답 카드
- ⚠️ **조작은 드래그가 아니라 탭-탭 방식** (태블릿 오조작/스크롤 충돌 방지)
  - 문제 카드 탭 → 선택 상태(테두리 강조) → 답 카드 탭 → 연결선 그려지며 판정
  - 선택 상태에서 같은 카드 재탭 = 선택 해제
- 웨이브: 1) 3vs3 → 2) 4vs5(함정1) → 3) 5vs6(함정2) → 4) 6vs7(함정1)+**30초 제한**
- 정답 매칭: **50점 + (콤보 × 5)**, 오답: 라이프 -1
- 5연속 정답: `CHAIN!` 점수 2배
- 웨이브 완료: +200점

**튜토리얼**: "문제를 누르고, 맞는 답을 눌러서 짝을 지어줘!"

---

### 6️⃣ 🏗️ 스택 빌더 — 축적형
**Phase 2 / 출제 상한 Lv4**

- 정답 블록만 탭해서 탑을 쌓는다
- ⚠️ **핵심 긴장 요소 (반드시 구현)**: **정답 블록이 바닥에 닿으면 놓친 것 → 라이프 -1**
  - 이게 없으면 학생이 그냥 기다리기만 하면 되므로 게임이 성립하지 않는다
- 오답 블록을 탭하면 탑이 기울어짐. **오답 3개 누적 → 탑 붕괴 = 웨이브 실패**
  - 기울기는 시각적으로 표현 (5도 → 12도 → 20도 → 붕괴)
- 웨이브 목표 높이: 5 → 8 → 12 → 15 → 20칸(이후 무한)
- 블록 1개: **10점**, 웨이브 완료: `목표높이 × 50`, 무오답 웨이브: +200점
- 콤보 상승 시 헷갈리는 숫자 등장 (48, 42, 54, 49)
- 콤보 10 연출: `STEADY!`

**튜토리얼**: "답이 맞는 블록만 눌러서 탑을 쌓아! 놓치면 안 돼!"

---

### 7️⃣ 💣 슈팅 계산 — 액션형
**Phase 4 / 출제 상한 Lv3**

- 하단에 캐릭터, 상단에서 숫자 적이 내려옴. **정답 적만 격추**
- ⚠️ **조작 방식 (초등생 배려)**: **자동 발사 + 좌우 이동만 조작**
  - 캐릭터는 0.4초마다 자동으로 위로 발사
  - 학생은 좌우 이동만 신경쓰면 됨 (화면 좌/우 절반 터치 or 좌우 드래그, 키보드 ← →)
  - 수동 발사 방식은 오폭이 잦아 채택하지 않음
- 라운드별 적 3 → 6개, 하강 속도 상승
- 정답 격추: **50점 + (콤보 × 10)**
- 오답 격추 / 정답 적이 바닥 도달: 라이프 -1
- 콤보 10 `명중왕!`, 콤보 20 `무적포격!`
- ❌ 색 반전·화면 어둡게 금지. 난이도는 **속도와 혼동값으로만** 조절

**튜토리얼**: "좌우로 움직여서 정답 적을 맞혀! 총알은 자동으로 나가!"

---

### 8️⃣ 🔗 배수 체인 — 연쇄형
**Phase 3 / 출제 상한 Lv4**

- **곱셈 웨이브**: "4단의 배수를 순서대로 이어라!"
  - 화면에 숫자 버블이 떠다님: 4단 배수(4,8,12,16,20…) + 함정 비배수(14,18,22…)
  - 작은 수부터 순서대로 탭 → 초록 체인 연결
- **나눗셈 웨이브 (웨이브 절반)**: "6으로 나누어떨어지는 수를 찾아라!"
  - 나눗셈 개념과 배수 개념을 연결하는 것이 이 게임의 교육적 목적
- 함정 탭 or 순서 건너뜀: 체인 끊김 + 라이프 -1
- 체인 3칸 80점 / 5칸 200점 `CHAIN×5` / 10칸+ 3배 `ULTIMATE CHAIN!`
- 웨이브마다 단(dan)이 바뀌고 함정 수·이동 속도 증가

**튜토리얼**: "4, 8, 12… 순서대로 눌러서 길게 이어봐!"

---

### 9️⃣ 🎈 벌룬 팝 — 개념 확장형
**Phase 2 / 출제 상한 Lv3**

- ⚠️ **2번 게임과 차별화 필수**: 단순히 방향만 반대인 게임이 되면 안 된다.
- **핵심 메커닉: 값이 같은 풍선을 전부 찾기**
  - 문제: `6 × 4 = ?` → 답은 24
  - 떠오르는 풍선: `24`, `8×3`, `48÷2`, `4×6` … **값이 24인 풍선을 전부 터뜨려야 클리어**
  - 오답 풍선: `26`, `12×3`, `20` 등
  - 즉 "식의 값이 같다"는 개념(등식)을 자연스럽게 학습
- 한 라운드에 정답 풍선 2~4개, 전부 터뜨리면 다음 문제
- ⚠️ **콤보는 '라운드 완성' 단위로 오른다** (개별 풍선 팝마다가 아님). 값이 같은 풍선을 **전부** 터뜨려 라운드를 완성해야 콤보 +1. 게임 간 콤보(=연속 성공)의 의미를 통일하기 위함이다. 점수는 개별 팝마다 `70 + 콤보×8`로 계산해 누적하고 라운드 완성 시 반영한다. 오답 풍선/정답 풍선 탈출은 라운드 실패 → 콤보 리셋.
- 정답 풍선: **70점 + (콤보 × 8)**
- 오답 풍선 터뜨림: 라이프 -1
- **정답 풍선이 화면 밖 탈출: 콤보 리셋 (라이프 유지)**
- 상승 속도: 콤보 5마다 ×1.15 (상한 있음)
- 연출: 콤보 5 `BURST!`, 15 `EXPLOSION!`, 25 `FIREWORKS!`
- **2초 내 3개 연속**: `RAPID FIRE!` +200점
- ❌ 화면 어둡게 하기 금지. 난이도는 풍선 수·속도·근접 오답으로만

**튜토리얼**: "답이 같은 풍선을 모두 찾아서 터뜨려!"

---

### 🔟 💎 나머지 보물찾기 — 나눗셈 심화
**Phase 5 / 출제 상한 Lv5**

- ⚠️ **이 게임만 개념 설명 화면을 별도로 둔다** (나머지 개념 최초 도입)
  - 그림: 사탕 17개를 5개씩 묶는 애니메이션 → 3묶음 + 2개 남음
  - "나누고 남은 것을 **나머지**라고 해!"
  - 이 화면은 첫 진입 시 1회, 이후 튜토리얼 화면에서 [개념 다시보기] 버튼으로 재접근
- **2단계 진행**
  1. **몫 선택**: 보물상자 3~4개에 후보 숫자 → 정답 상자 탭 → 상자 열림
  2. **나머지 선택**: 열린 상자에서 보석 후보 등장 → 정답 나머지 탭
- 몫 정답 **60점**, 나머지까지 완답 **+80점** + 보석 파티클
- **나머지 0인 문제 20% 확률 혼합** → `나머지 없음` 버튼 포함
- Lv 상승: 두 자리÷한 자리 (74÷9=8…2), 제한시간 8초 → 5초
- 몫/나머지 오답: 라이프 -1 + **올바른 식 전체 표시** (`17 ÷ 5 = 3 … 2` / `17 = 5 × 3 + 2`)

**튜토리얼**: "먼저 몫을 고르고, 그다음 남은 수를 골라줘!"

---

## 5. Phase 개발 순서

| Phase | 내용 | 완료 조건 |
|---|---|---|
| **0** | core 전체 + 메뉴 + 튜토리얼 + 결과화면 + **더미 게임 1개** | 더미 게임으로 전체 흐름(메뉴→튜토→플레이→오답피드백→결과→메뉴)이 태블릿에서 완주됨 |
| **1** | 1️⃣ 콤보 챌린지, 2️⃣ 떨어지는 캐치 | core 수정 없이 두 게임 추가 완료 |
| **2** | 9️⃣ 벌룬 팝, 6️⃣ 스택 빌더 (부양/낙하 물리 공유) | |
| **3** | 4️⃣ 타이밍 퍼즐, 8️⃣ 배수 체인 | |
| **4** | 5️⃣ 숫자 매칭, 7️⃣ 슈팅 계산 | |
| **5** | 3️⃣ 레이싱, 🔟 나머지 보물찾기 (예외 규칙 많음, 마지막) | |
| **6** | 리포트 UI 고도화, 빌드 스크립트, **폰 대응(논리 캔버스 가변화)**, 최종 다듬기 | `dist/index.html` 단일 파일 산출 |

> 참고: 교사 설정과 학습 리포트의 기본 동작은 Phase 0에서 이미 구현됨(효과음 OFF·레벨 고정 검증 필요). Phase 6은 리포트 화면의 시각화 고도화와 최종 빌드에 집중한다.

**각 Phase 종료 시 반드시**: 실제 갤럭시 탭에서 실행 → 문제 없으면 커밋 → 다음 Phase

---

## 6. 하지 말아야 할 것 (Claude Code 주의사항)

- ❌ 여러 Phase를 한 번에 진행하지 말 것. 지시받은 Phase만.
- ❌ 기존 게임 파일이나 core 파일을 임의로 수정하지 말 것.
- ❌ `// TODO`, `// 구현 예정`, 플레이스홀더 함수 금지. 만들면 끝까지 만든다.
- ❌ 외부 라이브러리·CDN·이미지·오디오 파일 추가 금지.
- ❌ 이 SPEC에 없는 기능을 임의로 추가하지 말 것. 필요하면 먼저 물어본다.
- ❌ 화면을 어둡게 하거나 색 반전하는 연출 금지.
- ❌ 효과음 기본값을 ON으로 바꾸지 말 것.

---

## 7. 확정된 게임 인터페이스 (Phase 0에서 확정 — Phase 1부터 이것만 보고 붙인다)

> ⚠️ 이 인터페이스는 확정이다. Phase 1~5의 모든 게임은 아래 형태를 그대로 구현하고,
> `core` 파일과 기존 게임 파일은 **절대 수정하지 않는다.** core 변경이 필요하면 먼저 물어본다.

### 7.1 게임 객체 형태
```js
export const g01Combo = {
  id: 'g01_combo',        // 고유 id (localStorage 키에 사용)
  name: '콤보 챌린지',
  emoji: '⚡',
  category: '선택형',
  maxLevel: 4,            // 출제 레벨 상한 (SPEC 2.1 표 — 반드시 지킬 것)

  comboMilestones: { 5: 'BURST!', 15: 'EXPLOSION!' },  // 선택적. 게임 고유 콤보 문구.
  //   지정한 콤보에서 core 기본 문구(GREAT!/PERFECT!/AMAZING!/LEGEND!) 대신 이 문구를 표시한다.
  //   지정하지 않은 콤보는 core 기본 문구를 그대로 쓴다. 필드가 없는 게임은 기존과 동일(하위 호환).
  //   ⚠️ 게임이 콤보 문구를 직접 showComboText 로 띄우지 말 것 — 중복 방지를 위해 core가 일원 관리한다.

  tutorial: {
    text: '한 문장 설명',
    draw(ctx) { /* 그림 한 장. ctx는 논리 좌표 800×1280. translate(0,260)된 카드 영역 안에서 그림 */ },
  },

  init(engine) { /* 상태 초기화. 첫 문제 로드. engine 저장 */ },
  update(dt)   { /* dt: 초 단위. freeze 중에는 호출되지 않음 */ },
  render(ctx)  { /* 논리 좌표로 그린다. HUD/오버레이는 엔진이 그림 */ },
  onTouch(x, y, phase) { /* phase: 'start'|'move'|'end'. x,y는 논리 좌표. freeze 중 미호출 */ },
  onKey(e)     { /* 선택적. 키보드 */ },
  onHover(x, y){ /* 선택적(마우스 전용). (x,y)가 클릭 가능 요소 위면 true 반환 → 엔진이 커서 pointer.
                   구현 안 해도 정상 동작. 터치에서는 호출되지 않음(hover 잔상 방지). */ },
  clearHover() { /* 선택적. hover 시각 상태 초기화(터치 시작·마우스 이탈 시 엔진이 호출) */ },
  destroy()    { /* 정리 */ },
};
```
등록: `src/games/registry.js`의 `IMPLEMENTED` 배열에 한 줄 추가하는 것이 전부다.

**마우스 hover/커서 (SPEC 2.5 접근성 · PC 확인용):**
- 엔진이 마우스 이동 시 `engine.dispatchHover(x, y)`를 호출한다. 현재 씬/게임의 `onHover(x,y)`(있으면)를
  불러 반환값이 truthy면 캔버스 커서를 `'pointer'`, 아니면 `'default'`로 바꾼다. ⏸ 버튼·일시정지 메뉴는
  엔진이 직접 판정한다.
- **터치 입력은 hover를 발생시키지 않는다.** `touchstart` 시 엔진이 `clearHover()`로 커서·hover 상태를
  초기화하고, 터치 직후 합성 마우스 이벤트는 input.js가 무시한다(hover 잔상 방지).
- hover는 **선택적**이다. `onHover`가 없는 게임/씬은 커서가 항상 `'default'`로 유지되며 정상 동작한다.

### 7.2 engine이 게임에 제공하는 것 (게임은 이것만 쓴다)
| 접근 | 용도 |
|---|---|
| `engine.problemGenerator.nextProblem({ maxLevel })` | 다음 문제. `{a,b,op,answer,remainder,level,text,blank,fromReview}` |
| `engine.problemGenerator.makeDistractors(problem, count, closeness)` | 오답 배열(0~1, 클수록 근접) |
| `engine.scoreManager.combo / .score / .lives / .maxCombo` | 읽기 전용으로 참고 (게임 난이도 축 B 계산에 combo 사용) |
| `engine.particles.emit(x, y, preset, color, count)` | preset: 'explode'/'pop'/'sparkle'/'gem' |
| `engine.sound.play(name)` | 'correct'/'wrong'/'combo'/'fanfare'/'tick'/'pop' (OFF면 무음) |
| `engine.ui.showComboText(text, big)` | 커스텀 콤보 문구 (게임 고유 문구 우선) |
| `engine.settings.timeScale` | 제한시간 배율 (제한시간을 쓰는 게임은 곱해서 사용) |

### 7.3 정답/오답 처리 — 반드시 이 두 메서드를 호출한다
게임은 점수·콤보·라이프·복습큐·레벨조정·연출을 **직접 만지지 않는다.** 아래만 호출하면 전부 처리된다.

```js
// 정답: 점수 가산 + 콤보 +1 + 라이프 회복(마일스톤) + 콤보연출 + 복습큐 제거 + 레벨상향 + 정답음
engine.answerCorrect(problem, userAnswer, points);
//   points = 게임별 기본점 + combo × 게임별 계수  (SPEC 3.4 / 각 게임 명세)

// 오답/시간초과: 콤보 리셋 + (옵션)라이프 -1 + 정답 1.2초 전체정지 표시 + 복습큐 추가 + 레벨하향 + 실패음
engine.answerWrong(problem, userAnswer, {
  loseLife: true,               // 반사신경 게임에서 '놓침'은 false (콤보만 리셋)
  onResume: () => this.next(),  // 1.2초 정지 종료 후 실행(다음 문제 로드 등). 라이프0이면 자동 RESULT
  freeze: true,                 // (기본 true) false면 1.2초 정답표시·화면정지·실패음 생략 → 게임을 멈추지 않음
                                //   반사신경 '놓침'용. freeze:false면 onResume은 쓰지 않고 게임이 스스로 진행
  affectLevel: true,            // (기본 true) false면 레벨 하향·복습큐 등록을 건너뜀 (놓침 = 반응속도 문제, 난이도 근거 아님)
  missed: false,                // (기본 false) 세션 기록에 '놓침' 여부 저장 → 결과 화면에서 오답 '터치'와 구분
});
engine.timeUp(problem, opts);   // 시간초과 = answerWrong(userAnswer=null) 별칭
```
- **오답 후 1.2초 동안 `update`/`onTouch`는 호출되지 않는다.** 게임 상태를 스스로 멈출 필요 없음. (단 `freeze:false`면 정지가 없으므로 게임이 다음 진행을 직접 이어간다.)
- 라이프가 0이 되면 1.2초 표시 후 엔진이 **자동으로 RESULT**로 전환한다. 게임이 endGame을 부르지 않는다.
- 게임이 자체적으로 게임오버를 내야 하면 `engine.endGame()` 호출.

### 7.4 좌표/입력/렌더 규약
- 모든 좌표는 **논리 800×1280**. 터치는 이미 논리 좌표로 변환되어 들어온다.
- ⚠️ **모든 좌표·크기·폰트는 `core/layout.js`의 `L` 헬퍼로 계산한다** (`L.x/y/w/h(ratio)`,
  `L.font(ratio)`, `L.safe`, `L.minTouch`, `L.zone.*`, `L.gu(n)`). **절대 픽셀 리터럴 사용 금지** (SPEC §1.2 반응형 원칙).
  - **가능하면 의미가 드러나는 `L.zone.*`(명명된 영역)와 `L.gu(n)`(그리드 단위)를 쓴다.**
    raw 비율(`L.y(0.117)` 같은)은 zone/gu로 **표현이 안 될 때만** 예외적으로 쓴다.
- 터치 타겟 최소 96×96(`L.minTouch`), 문제 텍스트 최소 80px, 안전 여백 24px(`L.safe`).
- HUD(점수/콤보/라이프/⏸)와 콤보·정답 오버레이, 파티클은 **엔진이 그린다.** 게임은 플레이 영역만 그린다.
- 상단 약 120px(HUD)와 그 아래는 게임이 자유롭게 사용.

### 7.5 축 분리 규칙 재확인 (SPEC 2.1)
- 수학 난이도(level)는 `ProblemGenerator`만 관리. 게임은 level 값을 읽지 않는다.
- 게임 난이도(속도/개수/오답근접도)는 게임이 `engine.scoreManager.combo`를 보고 자체 계산.
