# 나중에 처리할 것

## 다음 작업
- [x] 오답지 자릿수 버그: 답이 한 자리인데 오답이 세 자리로 나옴 (36÷9=4 → 104)
      → makeDistractors에 자릿수 일치 필터 도입. 나눗셈 오답은 몫 기준으로만 생성(피제수 사용 제거).
- [x] 게임별 연산 고정 (곱셈 전용 / 나눗셈 전용 분리)
      → 게임 객체 opMode 필드 + problemGenerator 반영. g01/g02/g09=곱셈, g06=나눗셈.
- [ ] 메뉴 게임 버튼에 연산 배지(× / ÷) 표시 — menuScene.js 수정 필요(승인 대기)
      배지 데이터(opMode)는 준비됨. menuScene._build에서 getGameById(id).opMode를 셀에 싣고,
      render에서 활성 셀 우측 상단에 그리면 됨. CATALOG에 opMode를 넣거나 게임 객체에서 조회.

## Phase 2.5 재미 실험에서 함께 처리
- [ ] 벌룬 팝: 풍선을 터뜨려도 점수판이 즉시 안 움직임
      (라운드 완성 시 일괄 반영 구조 때문. core에 콤보와 분리된 점수 가산 경로 필요)

## Phase 6
- [ ] g01_combo.js, g02_catch.js 절대 좌표를 L 헬퍼로 치환
      (헬퍼 도입 이전에 작성된 파일. 위반 지점 약 30곳)
      ※ g06_stack.js, g09_balloon.js 는 처음부터 L 기반이라 치환 불필요
- [ ] 폰트 크기 비율화 (반응형 원칙 3)
- [ ] ui.js의 SAFE 상수를 L.safe로 통합
- [ ] 논리 캔버스 가변화 (화면 비율 프로필 A/B/C)
- [ ] 단일 HTML 빌드 (build.js에 인라인 병합 로직 추가)
